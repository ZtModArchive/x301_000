
// This is a fixed function effect, so it doesn't
// care whether we are using vertex color or material color
// as our source. That's handled with states for FFP,
// whereas with shaders it requires separate code paths.
#include "Base_fres.fx" 

